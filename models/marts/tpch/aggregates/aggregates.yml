version: 2

models:
  - name: monthly_gross_revenue
    columns:
      - name: ORDER_MONTH
        tests:
          - unique
          - not_null
      - name: GROSS_REVENUE
        tests:
          - not_null
          - greater_than