name: pii_masking
version: 1.5.0

models:
  pii_masking:
    +materialized: table
    +tags: pii
    +docs:
      node_color: "#9467BD"

    sensitive:
      +group: sensitive
      +schema: sensitive_data

    sanitized:
      +tags: sanitized_pii
      +group: sanitized
      +schema: sanitized_sensitive_data